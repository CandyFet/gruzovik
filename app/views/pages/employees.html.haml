= render 'shared/catalog_form'

.simple-page
  %h1 Сотрудники

  = render 'menu_company'

  .employees-list.clearfix

    - @employees.each do |employee|
      .employee{class: cycle('first', 'second', name: 'employees-list')}
        .image= link_to image_tag(employee.image.preview), image_path(employee.image.url), data: {lightbox: 'employees'}, title: employee.name
        .content
          .name= employee.name
          - if employee.post?
            .post= employee.post
          - if employee.work_since?
            .since= employee.work_since
          - if employee.preview?
            .preview= employee.preview
      = cycle('', '<div class="clear"></div>'.html_safe, name: 'employees-list-clear')

